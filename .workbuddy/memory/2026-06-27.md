# 2026-06-27

- 克隆 cat-catch 到工作区，分析源码结构（抖音过滤四层拦截机制）
- 安装 codebase-cartographer skill，为 cat-catch 生成 7 模块 codemap
- Fork 到 Heyana/cat-catch，设置 upstream 远程，移除抖音域名过滤
- 创建 `optimize-download-pipeline` 分支，重构下载管线（6 次迭代 10 个 commit）：
  - **最终状态**：普通下载零跳转（background 直下）、M3U8 零跳转（offscreen 引擎）、FFmpeg 合并后台标签
  - 去 localStorage hack、移除 createCatDownload、getUrlFileName 修 NULL bug
  - 放弃：自动关闭（用户不要）、offscreen 内 CORS/fetch/SW 的 URL.createObjectURL/chrome.offscreen API 限制
- 经验：offscreen 仅可用 chrome.runtime/storage/DOM，其余 API 须经 background 转发；SW 无 URL.createObjectURL
